defaults:
  - experiment # inheriting from experiment config
  - _self_ # for hydra 1.1 compatibility
  - override hydra/job_logging: colorlog # colorful logging
  - override hydra/hydra_logging: colorlog # colorful logging

hydra:
  run:
    dir: runs/${experiment_name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  sweep:
    dir: sweeps/${experiment_name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  job:
    chdir: true # to change the working directory during the run/sweep directory
